import { YearsSinceDatePipe } from './years-since-date-pipe';

describe('YearsSinceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new YearsSinceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
